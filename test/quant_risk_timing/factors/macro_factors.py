"""Placeholder module for future medium/long-term macro factors."""

from __future__ import annotations

import pandas as pd


def add_macro_factors(factor_df: pd.DataFrame) -> pd.DataFrame:
    """Return factor data unchanged until macro factors are introduced."""

    return factor_df.copy()
